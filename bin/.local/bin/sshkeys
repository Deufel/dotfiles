#!/bin/bash

echo -e "── SSH Keys ──────────────────"
ls -la ~/.ssh/ | grep -v "^total\|^\.\.$\|^\.$"

echo -e "\n── Loaded in Agent ───────────"
ssh-add -l 2>/dev/null || echo "No keys loaded / agent not running"

echo -e "\n── Config ────────────────────"
cat ~/.ssh/config
